# Optional: enable Tumblr sync for all users. Set these once (deployer only).
# Create one app at https://www.tumblr.com/oauth/apps — users then just "Sign in with Tumblr"; no keys for them.
TUMBLR_CONSUMER_KEY=
TUMBLR_CONSUMER_SECRET=

# Optional: default blog name (users can still sync any blog)
TUMBLR_BLOG=

# Session secret for OAuth (set in production)
FLASK_SECRET_KEY=
